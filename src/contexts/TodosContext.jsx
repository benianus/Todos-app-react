import { createContext } from "react";

export let TodosContext = createContext([]);
export let InputContext = createContext("");
export let SnackbarContext = createContext(false);
export let TabContext = createContext(2);
